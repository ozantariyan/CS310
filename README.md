# Flutter Stock Tracker

This repository contains a minimal Flutter application for tracking stock quantities of items.

## Getting Started

1. Install [Flutter](https://flutter.dev/docs/get-started/install) on your machine.
2. Navigate to this directory and run `flutter pub get` to fetch dependencies.
3. Launch the application with `flutter run`.

The main application code is located in `lib/main.dart`.
